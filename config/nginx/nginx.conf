# /etc/nginx/nginx.conf
# Optimized nginx configuration for media server with Authelia authentication

# Main context
user nginx;
worker_processes auto;
worker_rlimit_nofile 65535;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

# Events block
events {
    worker_connections 4096;
    multi_accept on;
    use epoll;
    epoll_events 512;
}

# HTTP block
http {
    # MIME types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # ============================================================================
    # INCLUDE MAPS - CENTRALIZED IN MAPS.CONF
    # ============================================================================
    include /etc/nginx/includes/maps.conf;

    # ============================================================================
    # LOGGING CONFIGURATION
    # ============================================================================

    # Enhanced logging format
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                   '$status $body_bytes_sent "$http_referer" '
                   '"$http_user_agent" "$http_x_forwarded_for" '
                   'rt=$request_time uct="$upstream_connect_time" '
                   'uht="$upstream_header_time" urt="$upstream_response_time" '
                   'client_type="$client_type"';

    # Additional logging format for debugging
    log_format debug '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for" '
                    'rt=$request_time uct="$upstream_connect_time" '
                    'uht="$upstream_header_time" urt="$upstream_response_time" '
                    'host="$host" upstream="$upstream_addr" '
                    'cache="$upstream_cache_status"';

    # Use conditional logging based on should_log map
    access_log /var/log/nginx/access.log main if=$should_log;
    error_log /var/log/nginx/error.log warn;

    # ============================================================================
    # PERFORMANCE SETTINGS
    # ============================================================================

    # Basic Performance Settings
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 75s;
    keepalive_requests 1000;
    types_hash_max_size 2048;
    server_tokens off;

    # Client settings - Optimized for media streaming
    client_body_timeout 60s;
    client_header_timeout 60s;
    send_timeout 60s;
    reset_timedout_connection on;

    # Buffer settings - Optimized for media server
    client_body_buffer_size 128k;
    client_header_buffer_size 4k;
    client_max_body_size 1G;
    large_client_header_buffers 8 16k;

    # Proxy buffer settings - Critical for streaming
    proxy_buffering on;
    proxy_buffer_size 8k;
    proxy_buffers 16 8k;
    proxy_busy_buffers_size 16k;
    proxy_temp_file_write_size 16k;
    proxy_max_temp_file_size 1024m;

    # Proxy timeout settings - Important for media streaming
    proxy_connect_timeout 60s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;
    proxy_next_upstream error timeout http_502 http_503 http_504;

    # ============================================================================
    # COMPRESSION SETTINGS
    # ============================================================================

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 32 8k;
    gzip_http_version 1.1;
    gzip_min_length 1024;
    gzip_types
        application/atom+xml
        application/geo+json
        application/javascript
        application/x-javascript
        application/json
        application/ld+json
        application/manifest+json
        application/rdf+xml
        application/rss+xml
        application/xhtml+xml
        application/xml
        font/eot
        font/otf
        font/ttf
        image/svg+xml
        text/css
        text/javascript
        text/plain
        text/xml;

    # ============================================================================
    # CACHING SETTINGS
    # ============================================================================

    # File caching
    open_file_cache max=10000 inactive=20s;
    open_file_cache_valid 30s;
    open_file_cache_min_uses 2;
    open_file_cache_errors on;

    # Add cache control header based on content type
    add_header Cache-Control $cache_control_header;

    # ============================================================================
    # RATE LIMITING ZONES
    # ============================================================================

    # Updated rate limiting zones with better granularity
    limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/s;        # Auth endpoints
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;        # API endpoints
    limit_req_zone $binary_remote_addr zone=media:10m rate=20r/s;      # Media streaming
    limit_req_zone $binary_remote_addr zone=admin:10m rate=15r/s;      # Admin interfaces
    limit_req_zone $binary_remote_addr zone=general:10m rate=15r/s;    # General requests
    limit_req_zone $binary_remote_addr zone=health:10m rate=30r/s;     # Health checks

    # Connection limiting
    limit_conn_zone $binary_remote_addr zone=conn_limit_per_ip:10m;
    limit_conn_zone $server_name zone=conn_limit_per_server:10m;

    # Apply connection limits globally
    limit_conn conn_limit_per_ip 20;

    # ============================================================================
    # REAL IP CONFIGURATION
    # ============================================================================

    # Real IP configuration (important for Docker/proxy setups)
    real_ip_header X-Forwarded-For;
    real_ip_recursive on;
    set_real_ip_from 172.16.0.0/12;  # Docker network
    set_real_ip_from 10.0.0.0/8;     # Private networks
    set_real_ip_from 192.168.0.0/16; # Private networks

    # ============================================================================
    # SECURITY HEADERS (Applied globally using maps)
    # ============================================================================

    add_header X-Frame-Options $x_frame_options always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy $referrer_policy always;
    add_header Content-Security-Policy $content_security_policy always;
    add_header Strict-Transport-Security $hsts_duration always;

    # Additional security headers
    add_header X-Robots-Tag "noindex, nofollow, nosnippet, noarchive" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # ============================================================================
    # UPSTREAM DEFINITIONS
    # ============================================================================

    upstream authelia {
        server 100.121.199.80:9091;
        keepalive 2;
    }

    upstream emby {
        server 100.86.22.59:8096;
        keepalive 2;
    }

    upstream jellyfin {
        server 100.86.22.59:8096;
        keepalive 2;
    }

    upstream plex {
        server 100.86.22.59:32400;
        keepalive 2;
    }

    upstream sonarr {
        server 100.86.22.59:8989;
        keepalive 2;
    }

    upstream radarr {
        server 100.86.22.59:7878;
        keepalive 2;
    }

    upstream lidarr {
        server 100.86.22.59:8686;
        keepalive 2;
    }

    upstream jackett {
        server 100.86.22.59:9117;
        keepalive 2;
    }

    upstream qbittorrent {
        server 100.86.22.59:8080;
        keepalive 2;
    }

    upstream audiobookshelf {
        server 100.86.22.59:80;
        keepalive 2;
    }

    upstream calibre {
        server 100.86.22.59:8081;
        keepalive 2;
    }

    upstream calibre_web {
        server 100.86.22.59:8083;
        keepalive 2;
    }

    upstream ollama {
        server 100.86.22.59:11434;
        keepalive 2;
    }

    upstream open_webui {
        server 100.86.22.59:8080;
        keepalive 2;
    }

    upstream portainer-sullivan {
        server 100.86.22.59:9000;
        keepalive 2;
    }

    upstream portainer-freddy {
        server 100.121.199.80:9000;
        keepalive 2;
    }

    upstream pihole {
        server 100.121.199.80:80;
        keepalive 2;
    }

    upstream filebot {
        server 100.86.22.59:5452;
        keepalive 2;
    }

    upstream duplicati {
        server 100.86.22.59:8200;
        keepalive 2;
    }

    upstream ytdl_material {
        server 100.86.22.59:17442;
        keepalive 2;
    }

    upstream mealie {
        server 100.86.22.59:9000;
        keepalive 2;
    }

    upstream grocy {
        server 100.86.22.59:80;
        keepalive 2;
    }

    upstream readarr_audio {
        server 100.86.22.59:8787;
        keepalive 2;
    }

    upstream readarr_ebooks {
        server 100.86.22.59:8787;
        keepalive 2;
    }

    # TODO: Add proper wiki upstream when you determine the backend
    # For now, commenting out the broken wiki config
    # upstream wiki {
    #     server wiki-service:port;
    #     keepalive 2;
    # }

    # ============================================================================
    # DEFAULT SERVERS
    # ============================================================================

    # Default server block to handle undefined server names
    server {
        listen 80 default_server;
        listen 443 ssl default_server;
        server_name _;
        
        # SSL certificate (even for default server)
        ssl_certificate /etc/nginx/ssl/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/privkey.pem;
        
        # Drop connections to undefined hosts
        return 444;
    }

    # Health check server (minimal, no auth needed)
    server {
        listen 8080;
        server_name localhost;
        
        access_log off;
        
        location /health {
            return 200 "OK\n";
            add_header Content-Type text/plain;
        }
        
        location /nginx-status {
            stub_status on;
            allow 127.0.0.1;
            allow 172.16.0.0/12;  # Docker networks
            deny all;
        }
        
        location / {
            return 404;
        }
    }

    # ============================================================================
    # INCLUDE SERVER CONFIGURATIONS
    # ============================================================================

    include /etc/nginx/conf.d/*.conf;
}